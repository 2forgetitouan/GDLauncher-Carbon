name: All OS

on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  mac_universal:
    defaults:
      run:
        working-directory: ./GDL
    runs-on: macos-14
    timeout-minutes: 80
    env:
      MACOSX_DEPLOYMENT_TARGET: "11.0"
    steps:
      - uses: actions/checkout@v4
        with:
          path: GDL
      - uses: pnpm/action-setup@v4
        with:
          version: "9.13.2"
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: "24.14.0"
          cache: "pnpm"
          cache-dependency-path: "GDL/pnpm-lock.yaml"
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.95.0
          targets: aarch64-apple-darwin, x86_64-apple-darwin
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "GDL -> GDL/target"
          save-if: ${{ github.ref == 'refs/heads/offline-support' || github.event_name == 'workflow_dispatch' }}
          cache-on-failure: true
      - name: Prepare
        run: |
          echo "" > crates/carbon_repos/src/db.rs
          cargo fmt --check
      - run: |
          npm run sudo-init
          chmod -R a+x node_modules
      - name: Iridium patchfix
        run: node iridium_patchfix.js
      - name: Build
        run: pnpm build:mac-universal-prod
        env:
          CARGO_INCREMENTAL: 0
          BASE_API: ${{ vars.BASE_API }}
          TEST_BASE_API: ${{ vars.TEST_BASE_API }}
          MS_AUTH_CLIENT_ID: ${{ vars.MS_AUTH_CLIENT_ID }}
          META_BASE_URL: ${{ vars.META_BASE_URL }}
          USER_AGENT_PREFIX: ${{ vars.USER_AGENT_PREFIX }}
          CURSEFORGE_API_KEY: "${{ vars.CURSEFORGE_API_KEY }}"
          CURSEFORGE_API_BASE: ${{ vars.CURSEFORGE_API_BASE }}
          CORE_MODULE_DSN: ${{ vars.CORE_MODULE_DSN || '' }}
      - name: Lint
        id: lint
        continue-on-error: true
        run: pnpm lint
      - name: Clear release artifacts to free disk before tests
        run: rm -rf target/release
      - name: Test
        id: test
        continue-on-error: true
        env:
          CARGO_INCREMENTAL: 0
          BASE_API: ${{ vars.BASE_API }}
          TEST_BASE_API: ${{ vars.TEST_BASE_API }}
          MS_AUTH_CLIENT_ID: ${{ vars.MS_AUTH_CLIENT_ID }}
          RUNTIME_PATH: ${{ vars.RUNTIME_PATH }}
          META_BASE_URL: ${{ vars.META_BASE_URL }}
          USER_AGENT_PREFIX: ${{ vars.USER_AGENT_PREFIX }}
          CURSEFORGE_API_KEY: "${{ vars.CURSEFORGE_API_KEY }}"
          CURSEFORGE_API_BASE: ${{ vars.CURSEFORGE_API_BASE }}
          CORE_MODULE_DSN: ${{ vars.CORE_MODULE_DSN || '' }}
        run: pnpm test:all
      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-macos
          path: ./GDL/apps/desktop/playwright-report/
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-macos
          path: ./GDL/apps/desktop/test-results/
      - name: Upload macOS build
        uses: actions/upload-artifact@v4
        with:
          name: gdlauncher-macos
          path: ./GDL/apps/desktop/release/*.dmg
      - name: Upload main.log for debugging
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-macos
          path: |
            ./GDL/apps/desktop/release/mac-universal/gdl_data/main.log
            ./GDL/apps/desktop/release/mac-universal/gdl_data/data/__gdl_logs__/*.log
      - name: Fail if lint failed
        if: steps.lint.outcome == 'failure'
        run: exit 1
  ubuntu_x64:
    defaults:
      run:
        working-directory: ./GDL
    runs-on: ubuntu-22.04
    timeout-minutes: 80
    steps:
      - uses: actions/checkout@v4
        with:
          path: GDL
      - uses: pnpm/action-setup@v4
        with:
          version: "9.13.2"
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: "24.14.0"
          cache: "pnpm"
          cache-dependency-path: "GDL/pnpm-lock.yaml"
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.95.0
          targets: x86_64-unknown-linux-gnu
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "GDL -> GDL/target"
          save-if: ${{ github.ref == 'refs/heads/offline-support' || github.event_name == 'workflow_dispatch' }}
          cache-on-failure: true
      - name: Install mold linker
        run: sudo apt-get install -y mold
      - name: Prepare
        run: |
          echo "" > crates/carbon_repos/src/db.rs
          cargo fmt --check
      - run: |
          npm run sudo-init
          chmod -R a+x node_modules
      - name: Iridium patchfix
        run: node iridium_patchfix.js
      - name: Build
        run: pnpm build:linux-x64-prod
        env:
          CARGO_INCREMENTAL: 0
          RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
          BASE_API: ${{ vars.BASE_API }}
          TEST_BASE_API: ${{ vars.TEST_BASE_API }}
          MS_AUTH_CLIENT_ID: ${{ vars.MS_AUTH_CLIENT_ID }}
          META_BASE_URL: ${{ vars.META_BASE_URL }}
          USER_AGENT_PREFIX: ${{ vars.USER_AGENT_PREFIX }}
          CURSEFORGE_API_KEY: "${{ vars.CURSEFORGE_API_KEY }}"
          CURSEFORGE_API_BASE: ${{ vars.CURSEFORGE_API_BASE }}
          CORE_MODULE_DSN: ${{ vars.CORE_MODULE_DSN || '' }}
      - name: Lint
        id: lint
        continue-on-error: true
        run: pnpm lint
      - name: Clear release artifacts to free disk before tests
        run: rm -rf target/release
      - name: Test
        id: test
        continue-on-error: true
        env:
          DISPLAY: ":1"
          CARGO_INCREMENTAL: 0
          BASE_API: ${{ vars.BASE_API }}
          TEST_BASE_API: ${{ vars.TEST_BASE_API }}
          MS_AUTH_CLIENT_ID: ${{ vars.MS_AUTH_CLIENT_ID }}
          RUNTIME_PATH: ${{ vars.RUNTIME_PATH }}
          META_BASE_URL: ${{ vars.META_BASE_URL }}
          USER_AGENT_PREFIX: ${{ vars.USER_AGENT_PREFIX }}
          CURSEFORGE_API_KEY: "${{ vars.CURSEFORGE_API_KEY }}"
          CURSEFORGE_API_BASE: ${{ vars.CURSEFORGE_API_BASE }}
          CORE_MODULE_DSN: ${{ vars.CORE_MODULE_DSN || '' }}
        run: |
          sudo apt-get install -y xvfb
          /usr/bin/Xvfb :1 -screen 0 1280x960x24 &
          export DISPLAY=:1
          sleep 5
          ps aux | grep Xvfb
          ls -l /tmp/.X11-unix/
          echo "DISPLAY=$DISPLAY"
          pnpm test:all
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: ./GDL/apps/desktop/playwright-report/
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-linux
          path: ./GDL/apps/desktop/test-results/
      - name: Upload Linux build
        uses: actions/upload-artifact@v4
        with:
          name: gdlauncher-linux
          path: ./GDL/apps/desktop/release/*.AppImage
      - name: Upload main.log for debugging
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-linux
          path: |
            ./GDL/apps/desktop/release/linux-unpacked/gdl_data/main.log
            ./GDL/apps/desktop/release/linux-unpacked/gdl_data/data/__gdl_logs__/*.log
      - name: Fail if lint failed
        if: steps.lint.outcome == 'failure'
        run: exit 1
  windows_x64:
    defaults:
      run:
        working-directory: ./GDL
    runs-on: windows-2022
    timeout-minutes: 80
    steps:
      - uses: actions/checkout@v4
        with:
          path: GDL
      - uses: pnpm/action-setup@v4
        with:
          version: "9.13.2"
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: "24.14.0"
          cache: "pnpm"
          cache-dependency-path: "GDL/pnpm-lock.yaml"
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.95.0
          targets: x86_64-pc-windows-msvc
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "GDL -> GDL/target"
          save-if: ${{ github.ref == 'refs/heads/offline-support' || github.event_name == 'workflow_dispatch' }}
          cache-on-failure: true
      - name: Install scoop and llvm
        run: |
          Invoke-Expression (New-Object System.Net.WebClient).DownloadString('https://get.scoop.sh')
          scoop install llvm
      - name: Prepare
        shell: bash
        run: |
          echo "" > crates/carbon_repos/src/db.rs
          cargo fmt --check
      - name: init
        shell: bash
        run: npm run init
      - name: Iridium patchfix
        shell: bash
        run: node iridium_patchfix.js
      - name: Build
        shell: bash
        run: pnpm build:win-x64-prod
        env:
          CARGO_INCREMENTAL: 0
          BASE_API: ${{ vars.BASE_API }}
          TEST_BASE_API: ${{ vars.TEST_BASE_API }}
          MS_AUTH_CLIENT_ID: ${{ vars.MS_AUTH_CLIENT_ID }}
          META_BASE_URL: ${{ vars.META_BASE_URL }}
          USER_AGENT_PREFIX: ${{ vars.USER_AGENT_PREFIX }}
          CURSEFORGE_API_KEY: "${{ vars.CURSEFORGE_API_KEY }}"
          CURSEFORGE_API_BASE: ${{ vars.CURSEFORGE_API_BASE }}
          CORE_MODULE_DSN: ${{ vars.CORE_MODULE_DSN || '' }}
          CARGO_NET_GIT_FETCH_WITH_CLI: true
      - name: Lint
        id: lint
        continue-on-error: true
        shell: bash
        run: pnpm lint
      - name: Clear release artifacts to free disk before tests
        shell: bash
        run: rm -rf target/release
      - name: Test
        id: test
        continue-on-error: true
        shell: bash
        env:
          CARGO_INCREMENTAL: 0
          BASE_API: ${{ vars.BASE_API }}
          TEST_BASE_API: ${{ vars.TEST_BASE_API }}
          MS_AUTH_CLIENT_ID: ${{ vars.MS_AUTH_CLIENT_ID }}
          RUNTIME_PATH: ${{ vars.RUNTIME_PATH }}
          META_BASE_URL: ${{ vars.META_BASE_URL }}
          USER_AGENT_PREFIX: ${{ vars.USER_AGENT_PREFIX }}
          CURSEFORGE_API_KEY: "${{ vars.CURSEFORGE_API_KEY }}"
          CURSEFORGE_API_BASE: ${{ vars.CURSEFORGE_API_BASE }}
          CORE_MODULE_DSN: ${{ vars.CORE_MODULE_DSN || '' }}
          CARGO_NET_GIT_FETCH_WITH_CLI: true
        run: pnpm test:all
      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-win
          path: ./GDL/apps/desktop/playwright-report/
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-windows
          path: ./GDL/apps/desktop/test-results/
      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: gdlauncher-windows
          path: ./GDL/apps/desktop/release/*.exe
      - name: Upload main.log for debugging
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-windows
          path: |
            ./GDL/apps/desktop/release/win-unpacked/gdl_data/main.log
            ./GDL/apps/desktop/release/win-unpacked/gdl_data/data/__gdl_logs__/*.log
      - name: Fail if lint failed
        if: steps.lint.outcome == 'failure'
        run: exit 1
